<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta.2/css/bootstrap.min.css" integrity="sha384-PsH8R72JQ3SOdhVi3uxftmaW6Vc51MKb0q5P2rRUpPvrszuE4W1povHYgTpBfshb"
        crossorigin="anonymous">
    <title>The Dojo Message Board</title>
</head>
<body>
    <h1>The Dojo Message Board</h1>
    <form action="/createMessage" method="post">
        <div class="form-group">
            <label for="name">Name:</label>
            <input type="text" class="form-control" id="name" name="name">
        </div>
        <div class="form-group">
            <label for="message">Message</label>
            <input type="text" class="form-control" id="message" name="message">
        </div>
        <input type="submit" value="Post a Message" class="btn btn-info" role="button">
    </form>
    <% if(typeof(errors) != 'undefined' ) { %>
        <% for (var x in errors) { %>
            <p>
                <%= errors[x].message %>
            </p>
        <% } %>
    <% } %>
    <hr>
    <% for(var i = 0; i < Message.length; i++) { %>
       
        <div class="alert alert-info">
            <strong>Name:</strong> <%= Message[i].name %>
            <br>
            <strong>Message:</strong><%= Message[i].message %>
        </div>
            <% for( var x = 0; x < Message[i]._comment.length; x++) {%>
                <p>
                    <b>Name:</b>
                    <%= Message[i]._comment[x].name %>
                </p>
                <p>
                    <b>Comment:</b>
                    <%= Message[i]._comment[x].comment %>
                </p>
            <% } %>
            <form action="/createComment" method="post">
                <input value="<%= Message[i]._id%>" type="hidden" name="_idMessage">
                <div class="form-group">
                    <label for="name">Name:</label>
                    <input type="text" class="form-control" id="name" name="name">
                </div>
                <div class="form-group">
                    <label for="comment">Comment</label>
                    <input type="text" class="form-control" id="Comment" name="comment">
                </div>
                <input type="submit" value="Post a Comment" class="btn btn-info" role="button">
            </form>
 
        <% } %>
</body>
</html>